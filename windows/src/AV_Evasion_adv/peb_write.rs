// //PEB overwriting code to overwrite the ImagePathName to make it undetectable inside the sysinternal tools.
// use std::{command, process::Command};

// pub fn peb_overwrite(){
//     let output = Command::new("cmd.exe")
//     .args("/C","./peb.exe");
//     //peb.exe should be the compiled version of the assembly inside the peb.asm
    
// }