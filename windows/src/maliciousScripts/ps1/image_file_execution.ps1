REG ADD "HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Image File Execution Options\notepad.exe" /v Debugger /d "cmd.exe"

#We set the cmd.exe as a debugger to the notepad.exe that starts cmd.exe instead of notepad.exe instead when we start the notepad.exe

